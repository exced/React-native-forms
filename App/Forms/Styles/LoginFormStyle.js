import { StyleSheet } from 'react-native'
import { Fonts, Colors, ApplicationStyles } from '../../Themes/'

export default StyleSheet.create({
    input: {
        borderStyle: 'solid',
        borderRadius: 20,
        borderWidth: 0.8,
        borderColor: 'rgba(38,25,12,0.7)',
        height: 40,
        marginBottom: 20,
        color: 'rgba(38,25,12,0.7)',
        paddingHorizontal: 10,
        fontWeight: 'bold'
    },
})
